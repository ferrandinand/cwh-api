version: '3'
services:
  mysql:
    image: mysql:5.7
    ports:
      - "3307:3306"
    volumes:
      - mysqldata:/var/lib/mysql
      - ./resources:/docker-entrypoint-initdb.d  
    environment:
      MYSQL_ROOT_PASSWORD: codecamp
      TZ: Asia/Kolkata

volumes:
  mysqldata:
    driver: local